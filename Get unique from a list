def unique(arr):
    l=[]
    for x in range(0,len(arr)):
        for y in range(x+1,len(arr)):
            if arr[x]==arr[y]:
                break
                
        else:
            l.append(arr[x])
    print(l) 
